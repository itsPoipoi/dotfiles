;; Caps to escape/control configuration for Kanata

(defsrc
  caps spc
)

(defalias
  escctrl (tap-hold-press 0 200 esc lctl)
  spcaltgr (tap-hold 0 150 spc ralt)
)

(defcfg
  process-unmapped-keys yes
)

(deflayer base
  @escctrl @spcaltgr
)
